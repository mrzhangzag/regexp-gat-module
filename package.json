{
  "name": "regexp-gat-module",
  "version": "0.0.4",
  "description": "RegExp module",
  "main": "dist/regExpModule.umd.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "vue-cli-service serve ./main.js --open",
    "build": "vue-cli-service build --name regExpModule --entry ./src/regExpModule.js --target lib"
  },
  "keywords": [
    "regExp"
  ],
  "author": "zag",
  "license": "MIT",
  "devDependencies": {
    "@vue/cli-service": "^3.8.0",
    "vue-template-compiler": "^2.6.10"
  }
}
